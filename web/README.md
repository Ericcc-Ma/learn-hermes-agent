# Learn Hermes Agent Web

这是 `learn-hermes-agent` 的网站入口，用 Next.js 展示教程路径、30 秒 Demo、Hermes 源码地图和快速开始命令。

## 本地运行

```sh
npm install
npm run dev
```

默认地址：

```text
http://localhost:3000
```

## 构建检查

```sh
npm run lint
npm run build
```

页面内容主要在 `src/app/page.tsx`，全局样式在 `src/app/globals.css`。
